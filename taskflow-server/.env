# .env — Store secret config here (never share this file publicly)
# This tells Express which MongoDB database to connect to
MONGO_URI=mongodb://127.0.0.1:27017/taskflow

# Port the server runs on
PORT=5000
